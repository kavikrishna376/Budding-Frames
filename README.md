# Booking page images

This folder is reserved for any custom graphics used on booking.html
(e.g. a background texture or hero image), matching the pattern used
in images/frames/. Nothing is required here today — the booking hero
uses a pure-CSS animated glow, no image assets, so the page works
out of the box.
